apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.wordwidget'
version = '0.1.0'

android {
  namespace "expo.modules.wordwidget"
  defaultConfig {
    versionCode 1
    versionName "0.1.0"
  }
}
