<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!--
    Drawing over other apps. Android only — iOS has no equivalent API at all.
    The user still has to grant this by hand in system settings; there is no
    runtime dialog for it.
  -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

  <application>
    <service
      android:name="expo.modules.wordoverlay.OverlayService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Periodic vocabulary flashcard shown over other apps, started and stopped by the user." />
    </service>
  </application>

</manifest>
